okay welcome to this session about the web SDK um today we are going to learn not only how to use the web SDK but also why do you want to use it for um and then in the second part we are going to see how the code translates how the glue code will be used to to make our very own controls before we get there let me present myself because I'm the newest member of soyo my name is Ricardo Cruz I've been working with web Technologies for more than 20 years um probably when soyo was born I was actually playing with Microsoft from page and drink Weaver and all this kind of nice tools back then at least I've been only using soil since 2018. um yeah I've discovered it because I was looking for a session sorry I was looking for a way to create a desktop applications and I'm trying to avoid uh playing with JavaScript um Ironically in March of this year I've joined the company to start helping with the web framework um yeah this is the first time I see my warm mates including Javier which is from Spain we met in the other day in the hotel sawyerweb is the easiest way to build web applications um I can say this because I've been playing with all of the Frameworks that you can probably know like probably on Rails younger Drupal laravel and on the front and sides I've been working with react angular view I've been working professional with all of them and when life is discovered soil I really liked it because he's so easy to create a more complex applications like for example a real-time chat where in other Solutions maybe it will be more trickier you will be have to create the websocket server and communicate with their front-end using I don't know angular or all these new technologies with soil is really easy because you will have everything together we can consider soil with a full stack framework when you work with soil web you will be you won't be doing only the front end or the browser side of the application but also the the back end I mean the the line between both is really blurry in soil web you don't think about you're working on the front and or the back end or you don't even talk about being a full stack developer like when you are working in other Technologies other web Technologies in the case of a soy web you will delegate all of the business logic to the bar to the backend that means when you press on a bottom lab on the browser it will send a notification to the backend and the backend it's going to to decide what to do with that event on it will update for example the a label of a web page the the part that I really like more about Social Web is you won't be required to build an API between the front end and the back end it's everything is coming from for free with with soil it will include you only you will only need to to think about the data that you are passing between the backend and the front end but you won't have to be thinking about building a whole rest API or something like that most of the case uh most of the time you want me to to be dealing with the web SDK except in the case that you need to delegate some responsibility to the to the browser when you want to execute the code on the browser this is the the case where you will really need to to use the web SDK instead of using the web SDK what you normally have to do is create custom controls using the containers so for example you can compose and create new controls composing for example our text field and a label which we are going to see in a moment these are repository that I've downloaded already to demonstrate this okay this is the example where you can create a new control by composing other playing consoles in this case this is just a web text area and a web label when whenever you update the the text area you will subscribe to that event and you will implement it and for example you can update the label by just updating the the amount of characters and um and showing the the limit and if you are exceeding that limit you can for example change the color of the label to Red only in the case that you are exceeding the limit the problem with this method is as you can see while our line piping it's not in it's not being updated in real time it will be sent to the browser the browser will decide what to do with this new data and it will be sending the reply to the to the browser so it's not really real time so in this very specific case you will be forced to use the web SDK if your requirements for these web applications uh it's telling you that you really need to to do it in real time this is the very same web component but being we are using the web SDK from a scratch and as you can see can be updated in real time in this case we are just executing all of the code in the browser and we are sending in the background we are sending the data to the server if you can't leave the with the latency that should be fine um you can create a simple component and you can leave it light up unless and you really need to to delegate that and responsibility to the browser in that case you have the option to get rid of that latency using the web SDK so when do you want to use the web SDK whenever you want to reduce the latency or when for example you want to integrate with a third-party Library like for example you want to install a charting Library a new one for example or we are creating an example for using the the bootstrap dependency that we have in soil web but using a component that it's not include in the so web framework like for example the toast notifications we are going to include an example for that using the web SDK in the next version of soil or maybe if you already know some JavaScript and you will want to have some fun and you can do it as well what are you going to need in order to create your first control you are going to need to learn JavaScript that's required but we really recommend to use type type script not only because it will be giving you compiler errors and a lot of help in order to continue working on your application but it will also autocomplete all of the web SDK methods that we are providing optionally also CSS if you want to create a Pixel Perfect application um also there are situations where you might think that you need to resolve something using JavaScript but CSS is really powerful to create Transitions and animations hovering effects it's really interesting to learn but I recommend it also as I said in the other slide soyo will take care of all of the communication layer you only need to think about the the message that you are passing from there from the to the back end and the other way around this is the the control in a nutshell when you create a control it will have a default internal state which translates to soil it's just you can think about it as a plane object with some properties and some default values on those properties or components it's going to render the HTML based on that internal state you can update that internal State at any time for example if I have a yellow cone and I pass this Json saying that I want to to have a blue cube instead we are going to pass a Json item to from the server to the front end and it will update the internal state from there the component is going to be responsible if we rendered vhtml to represent the new state and when I'm talking about a state you can think about properties of a plane class in the same you can do in the other way around you can create even listeners in JavaScript and send those events back to the to the back end to to our soil code soya provides a lot of methods and events that you can use in order to pass all these data so instead of learning how to create these HTTP rest API you will use this only these methods and I will put the code next to the this slide so you can map it to to some real code if we want to pass some data and update the state of the con of the control in the front end we will start by calling update control which is a method that will be given to you when you extend a class from from the web SDK UI component so if we go to the same example but the one that it's using the web SDK it has to extend the web SDK UI control whenever we update for example this text computer property we are going to call the update method method this will tell the framework that you really want to update the the state of the front end So eventually in the in the class we will receive a call to the serialize event where you will be able and you will have the opportunity to fill this Json item for example I'm setting the text I'm setting the text limit and allow exit limit property this will be converted to to some Json that we will receive on the front-end side of the application on the update control this is how it looks like in code this is the the front-end code and if we go to the update control method we will see the Constructor um the other part of the file later we will be receiving and the data can you see it properly we will be passing the the Json we will be updating the internal state of the JavaScript control at the end of the method we are going to call refresh refresh is going to tell again the the framework that we want to render the the application again based on the new state of the control um you should never call render directly you should call refresh because internally soyo will know how to do it in a way that it won't be calling it more than twice he's if he's not needed and on the render while this method looks really big the only part of the custom code is this one we are going the same as in the in the container example we are going just to update the the label and we are going to add a CSS class depending on if we are exceeding the text alignment or not if we want to pass data back to the backend we are going to to use the triggered server event method from the JavaScript or type a script in this case in order to create or to render all these control there are a lot of ways of doing it with JavaScript we are we can just write some planes HTML or we can create using the JavaScript API we can create elements for example the text area and the span all these elements come we will be giving us some events the same as in soil that we can subscribe and we can Implement those events so in the Constructor we can for example add an even listener for the input event um from there we can execute the triggered server event Only If For example in this case only if the the internal data is changing to avoid calling it more than needed the the way it works is pretty similar we will be writing it in JavaScript but it's pretty similar we are going to send the event using just the the name and a set of parameters like for example in this case the the new text value or for example if we are updating the the control and we are exceeding the limit we can also for example send a new event for example so we only need to think about the Json code I mean so the the Json message this will be received in our soil part of the code in the execute event where we can do a cannot switch to see if we are seeing a text change event or we are seeing a limit exceeded event and from there we can rewrite the event in order to to use it in the final example like here for example This Is The Life code um when we when we extend a class using the web SDK UI control we are receiving also all these events one of the most important one is the JavaScript class name event where we will be well it's a really huge name the only thing that we need to do is return the name of the class and we will be using the name space and the name of the the class that we will be giving in the in the Type X script file you will have to check the also the web SDK document to see why you should then use the soil name space and you should select uh your own name and space to avoid any conflict be between your controls and any other controls that can be used by your users then we have the session JavaScript URLs event and this one is where we will be sending the JavaScript code from the back end to the to the front button it can be a code from it can be our very own code or it could be a URL from a Serial third-party Library like I don't know Google Maps or whatever you need in my example what I'm using is just a constant that has all of the JavaScript code and we will see how to compile that and fill it in a second in the event what we are going to do is just creating a web file share property that we will be creating it only if it doesn't exist and then in the data we are we are going to fill it with the constant JavaScript data what is important here is to set the session to nil otherwise the first time I use here enters into your website it will work but if he refresh as the session doesn't match it won't work if you said the Assassin to nil it will be available to everyone and then you just have to return a set of urls including this one or any other required URLs the same as with CSS you can also draw the control in the layout in real time using the draw control in layout editor and it was it works pretty much like in like when you are working in a canvas or in the PDF class you will be just driving you know rectangles and texts and all that you change anything you won't have to refresh the the whole project it will be updated in real time well we are probably providing some SDK documents that you can check and they are included in the extras folder if you go to the web SDK you can check the web SDK examples where you can learn more about the extra events that we are not covering in this session but you know maybe in your particular case we can be useful like setting HTML in the header or receiving a specific require request from the browser you can Implement a lot of events and the documents are included in the in the extras folder the only thing um you really need to do after seeing this session if you want to start creating your controls is doing a lot of practice for example this has the repository linked in the in the PDF but um you can try to enhance it and you can try to recreate it and if you get it stuck and you can whenever you you need it you can open a new thread in the form where we have this really nice Community or you can even send me a direct message and I will be more than happy to to help before we get into the questions I really want to show you also how to compile this type of script that I've been working um this is a this is some this example the typoscript is coming from the examples folder in the typescript example you can see a really basic example where let me open it in webstorm where you can see the very Basics um you will receive also the autocomplete file inside this file you will you will have all the methods that the soil SDK it's providing to you for example if you want to create the parameters okay so on item give me a thing in my in my example project all this is coming from that file for example you can set and get and all this in order to compile it you have to go to the typescript folder and write TSC for typescript compiler and it will generate the corresponding Javascript file where you can for example copy and paste in the constant that we've seen when you are working with your own controls this process is a little bit too much the other way to to easier that process is to instead of calling TSC we can call it using the watch argument and it will be waiting for changes so for example if I change any line I mean if I delete not file and I save it it will detect the change and it will create the new file the new compiler file in soyo you can create a new build step to copy the the file there so for example after building you can go to the distribution folder in the compiler version and you can include that file so whenever you run the application it will take the file from here if we go to the JavaScript URLs method and we use this other way of doing it instead of creating a new web file instance we will use it by opening the file that we are including in our build so whenever we run the the the application it will use that file and you won't have to be copying and pasting from the compiled file into your project it will just work but if you are planning to distribute this control or your one you want to copy and paste it in another place and we don't care about that error whenever we want to distribute that control it's more easier to to to share it if you use the constant version so whenever you are ready to go you will have to copy your code from the compiler JavaScript file and you will include it in the constant so that way you can just copy the control and paste it in another place any questions I have a lot about JavaScript yeah you can use so the question is uh well well about this application I'm using a code editor for typescript but you can use for example Visual Studio code which it really supports very well typescript it has Support also for the how to complete and all these armies free to use and it's open source we have a storm I prefer it but you're not required to use a webstorm or any you can do it um yeah I don't know I prefer this one because I'm used to it but if you want to go for visual studio code I think most of the framework has been got using visual studio code questions well one of the things that I want to really clarify is we don't want to to force you to use the web SDK you are provided with with it if you find any limitation that is forcing you to to really use the the this browser technique to avoid the limitation of the latency for example the idea is to to use soya for everything not JavaScript or type a script well now we will fix the the map issue and you should be able to use all the the markers in the in the map in our in the meantime yeah in the meantime you can use the web SDK for anything you want like for example the you know creating a custom table or very specific HTML that um that your requirements um can you just include that there's another file alongside certainly when you are safe you have to to bundle it in a single JavaScript or in the in the in this coffee yeah yeah and if you need more files you can add them here also in these in this event okay more questions okay thank you very much thank you